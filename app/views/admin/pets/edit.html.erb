<div class="container mt-4">
  <div class="card shadow">
    <div class="card-header bg-warning text-dark">
      <h1 class="h3 mb-0">Editar Mascota: <%= @pet.name %></h1>
    </div>
    <div class="card-body">
      <%= render 'form', pet: @pet %>
    </div>
  </div>
</div>
