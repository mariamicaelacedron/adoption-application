<div class="container mt-4">
  <div class="card shadow">
    <div class="card-header bg-primary text-white">
      <h1 class="h3 mb-0">Agregar Nueva Mascota</h1>
    </div>
    <div class="card-body">
      <%= render 'form', pet: @pet %>
    </div>
  </div>
</div>
