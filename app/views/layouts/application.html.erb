<!DOCTYPE html>
<html>
<head>
  <title>Salvando Patitas</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.0/font/bootstrap-icons.css">
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
</head>
<body>
  <header>
    <nav>
      <%= link_to 'Inicio', root_path %>
      <%= link_to 'Donar', new_donation_path %>
      

      <% if user_signed_in? %>
        <%= link_to 'Cerrar sesión', destroy_user_session_path, data: { turbo_method: :delete } %>
      <% else %>
        <%= link_to 'Iniciar sesión', new_user_session_path %>
        <%= link_to 'Registrarse', new_user_registration_path %>
      <% end %>
    </nav>
  </header>
  
  <main>
    <% flash.each do |type, msg| %>
      <div class="alert alert-<%= type %>">
        <%= msg %>
      </div>
    <% end %>
    
    <%= yield %>
  </main>
  
  <footer>
    <p>Salvando Patitas &copy; <%= Time.current.year %></p>
  </footer>
</body>
</html>