<div class="container mt-4">
  <h1 class="mb-4">Mascotas Disponibles para Adopción</h1>

  <% if @pets.empty? %>
    <div class="alert alert-info text-center">
      <h4>Actualmente no tenemos mascotas disponibles</h4>
      <p>Vuelve a visitarnos pronto o contáctanos para más información.</p>
    </div>
  <% else %>
    <div class="row row-cols-1 row-cols-md-3 g-4">
      <% @pets.each do |pet| %>
        <div class="col">
          <%= render 'pet_card', pet: pet %>
        </div>
      <% end %>
    </div>
    
    <div class="mt-4">
      <%= paginate @pets %>
      <div class="text-center text-muted mt-2">
        <small>Mostrando <%= @pets.size %> de <%= @pets.total_count %> mascotas</small>
      </div>
    </div>
  <% end %>
</div>